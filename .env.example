# GitHub OAuth App (https://github.com/settings/developers)
# Copy to ".env" in the project root and fill in. ".env" is gitignored.
# GitFinder includes a public client ID by default; override it here when rotating apps.
# The OAuth app must have GitHub Device Flow enabled.
# GITFINDER_GITHUB_CLIENT_ID=

# Optional: collect the signed-in GitHub user's primary verified email.
# Defaults to https://satori-collect-emails.vercel.app/v1/ingest.
# GitFinder includes a public write-only ingest key by default; override it here when rotating keys.
# INGEST_API_KEY=
# GITFINDER_EMAIL_INGEST_URL=
